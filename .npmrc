auto-install-peers=true
strict-peer-dependencies=false
# Expo + React Native autolinking cannot traverse pnpm symlinks reliably.
# Using hoisted node-linker is the documented fix for Expo monorepos.
# https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
